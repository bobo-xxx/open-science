## ✨ Главное

- **Подписка xAI (Grok) через OAuth.** Одна учётная запись подписки работает со всеми тремя протоколами агентов — Claude Code (Anthropic Messages), OpenCode (Chat Completions) и Codex (Responses) — через API xAI Responses. В настройках и при первоначальной настройке доступен вход по коду устройства; основной процесс обновляет токены, локально оценивается количество токенов Anthropic, а моделью по умолчанию стала Grok 4.6. (#1554, #1556)
- **Управляемые Специалисты из Маркетплейса.** Пакеты, установленные из Маркетплейса, получают явный источник `marketplace`. Содержимое издателя доступно только для чтения, ручная перезапись ZIP-файлом запрещена, а для обновления требуется более высокая версия SemVer относительно точной базовой версии содержимого. Установленные пакеты теперь сгруппированы как «Все / Пользовательские / Маркетплейс / Встроенные» и имеют управляемое представление сведений. (#1600)
- **Отслеживание зависимостей между запусками Notebook.** Завершённые запуски Python и R анализируются внутри процесса с помощью tree-sitter WASM. Результаты, полученные из предыдущих состояний переменных, помечаются как `stale`, `clear` или `unknown`, поэтому они больше не будут молча выдавать устаревшее состояние за текущее. (#1553)
- **Немедленная отправка во время хода.** Отправка сообщения из очереди во время выполнения хода больше не прерывает текущий ход. Слой совместимости использует встроенное управление последующими сообщениями каждого фреймворка, когда оно доступно, и корректно переключается на резервное поведение, когда оно не поддерживается. (#1566, #1593, #1592, #1590)
- **Запуск с приоритетом сводки.** Метаданные запросов сеансов и использование токенов в каждом ходе материализуются в SQLite. При нормальном запуске читаются сводки, а файл сеанса загружается только при открытии или экспорте этого сеанса — вместо разбора всех файлов JSON сеансов. (#1618, #1631)

## 🚀 Новые возможности

- **Провайдер подписки xAI (Grok) через OAuth** с авторизацией устройства, обновлением токенов, локальной оценкой количества токенов и моделью Grok 4.6 по умолчанию. (#1554, #1556)
- **Управляемые Специалисты из Маркетплейса** с содержимым издателя только для чтения, обновлениями под защитой SemVer, управляемыми сведениями, редактируемыми копиями и специальными средствами удаления. (#1600)
- **Отслеживание зависимостей между запусками Notebook** для Python и R, включая псевдонимы, изменения, классы, объектные модели и эффекты распространённых научных библиотек. (#1553)
- **Немедленная отправка во время хода через встроенные последующие сообщения** с резервным поведением для каждого фреймворка и корректной обработкой ответов на запросы разрешений. (#1566, #1593, #1592, #1590, #1589)
- **Предварительный просмотр артефактов в сообщениях** для управляемых ссылок на файлы, изображений Markdown, стабильных идентификаторов артефактов/версий и созданных карточек. (#1587, #1597)
- **Улучшения предварительного просмотра Notebook** для графиков, результатов текущего сеанса, доступности Notebook и завершённых Notebook в режиме только для чтения. (#1605, #1564, #1545, #1599)
- **Границы сжатия контекста в расшифровке** с отдельными состояниями выполнения, завершения, сбоя и отмены. (#1581)
- **Отмена архивации с клавиатуры** с помощью стандартного сочетания отмены на рабочем столе. (#1595)
- **Новый раздел «О программе» и вход для обратной связи** со справочным центром и ресурсами примечаний к выпуску. (#1551, #1588)
- **Лимиты токенов пользовательских моделей** для контекста, входных и выходных данных с редактируемыми наборами параметров. (#1525, #1546)
- **Полный жизненный цикл OAuth-коннекторов** с авторизацией при первом сохранении, повтором, восстановлением, отменой и завершением позднее. (#1560, #1563)
- **deepseek-v4-flash-vision-exp** добавлена в каталог моделей DeepSeek. (#1538)

## 🔧 Улучшения

- Метаданные сеансов индексируются в SQLite для запуска с приоритетом сводки, а число просмотров истории в интерфейсе уменьшено. (#1618, #1631, #1626)
- Всплески инструментов и нетекстовые события в реальном времени обрабатываются пакетами для снижения нагрузки IPC на интерфейс. (#1557, #1555)
- Журналы установщика передаются пакетами, а объём их хранения в интерфейсе ограничен. (#1606)
- Повышена стабильность workers полного набора Vitest и снимков E2E для длинных диалогов. (#1625, #1627, #1628)
- Уточнены примечания в README для v0.18.2. (#1634)

## 🐛 Исправления ошибок

- **Идентичность Агентов и область возможностей** теперь правильно изолированы. (#1617)
- **Немедленная отправка в реальном времени** больше не оставляет операции возобновления или прерывания в зависшем состоянии. (#1613)
- **Ядра и вкладки Notebook** исправляют маршрутизацию ввода, одобренные состояния, ограничения вывода протокола, отклонение некорректных вызовов и граничные случаи сред Python/R. (#1604, #1619, #1615, #1612, #1570, #1571, #1569, #1616, #1621, #1568, #1540, #1537)
- **Ошибки провайдеров Codex и Claude** корректно обрабатывают несовместимые, временные, устаревшие и сохранённые подключения без лишних отчётов. (#1594, #1586, #1584, #1583)
- **Редактор и очередь** сохраняют команды с косой чертой, выбор Специалиста, сообщения допуска среды выполнения и сохранённые редакции плана. (#1633, #1630, #1601, #1610)
- **История делегирования** отмечает незавершённых импортированных Субагентов и предотвращает конфликты повторно воспроизведённых действий. (#1609, #1520)
- **Обновление и запуск** сохраняют передачу загрузки, разделяют проверки базы данных, поддерживают непрерывную загрузку и позволяют закрывать панели восстановления. (#1632, #1539, #1598, #1565, #1591)
- **Сеансы и рабочее пространство** исправляют область моделей, локальные заголовки, одновременные отправки в режиме ожидания, обновление доступности и восстановление переключения Специалиста. (#1552, #1579, #1577, #1602, #1543)
- **Настройки и отклонение устаревшего состояния** сохраняют оптимистичные параметры и отклоняют устаревшие снимки предварительного просмотра, тегов и обновления папок. (#1629, #1573, #1578, #1574, #1575)
- **Артефакты, вычисления, Коннекторы и навигация** улучшают восстановление, дедупликацию повторов, временные подключения MCP, выравнивание элементов управления Специалистами и локализацию неожиданных ошибок. (#1542, #1541, #1544, #1611, #1607, #1608, #1580)
- **Статический контекст Notebook** остаётся в пределах бюджета. (#1572)
