## ✨ 亮点

- **智能文献甄别实时运行。** 智能合集现在可以实时甄别参考文献，并提供暂停与继续控制，让大规模甄别任务自始至终尽在掌握。(#2968)
- **新连接器与比对工具。** HMMER 加入连接器家族，可运行面向特定程序的 EMBL-EBI HMMER3 同源性搜索（#2957）；InterProScan 作为连接器加入，可查询既有注释任务的状态并获取结果（#2935）；Genomes 连接器则新增了 Clustal Omega 多序列比对（#2944）。
- **服务商目录刷新。** GPT-6 与 Claude Opus 5.5 加入服务商目录，新配置与现有配置都可以直接选用。(#2967)
- **PDF 证据随对话同行。** 工作区对话现在可以在首条消息中一并携带 PDF 证据，让背景材料先于智能体开始工作之前送达。(#2941)

## 🚀 新功能

- 智能文献甄别实时运行：甄别任务随参考文献到达即时评估，并可在任意时刻暂停与继续。(#2968)
- HMMER 连接器：提交面向特定程序的 EMBL-EBI HMMER3 搜索——phmmer、hmmscan、hmmsearch 或 jackhmmer——为蛋白质序列、谱 HMM 和比对结果检索匹配的数据库。(#2957)
- InterProScan 连接器：通过任务 ID 查询既有 InterProScan 注释任务的状态并获取其结果。(#2935)
- Clustal Omega 多序列比对登陆 Genomes 连接器：使用 EMBL-EBI Clustal Omega 对三条及以上 FASTA 蛋白质、DNA 或 RNA 记录执行比对，并获取可下载的比对文件。(#2944)
- GPT-6 与 Claude Opus 5.5 模型加入服务商目录。(#2967)
- 工作区对话可以在首条消息中附带 PDF 证据，智能体开工前即可看到原始材料。(#2941)
- 会话诊断在你明确选择加入后可以包含敏感的包证据，为排障提供更深入的上下文。(#2947)
- 文献合集现在会区分并链接其作用域，个人合集与共享合集得以清晰地区分与关联。(#2938)

## 🔧 改进

- 设置的服务商区块标题新增了直接的服务商操作，添加或调整服务商所需的步骤更少。(#2970)
- 工作区文件换上了统一的文件类型图标，扫视混合文件夹时更加一目了然。(#2965)

## 🐛 问题修复

- **Notebook** —— Windows 托管 Python 运行时得到恢复，且托管 Python 路径在探测期间即被激活，应用托管环境在 Windows 上重新可用（#2953, #2951）；R 运行时访问被拒时，应用现在会弹出提示，而不是静默失败（#2930）。
- **会话** —— 运行时与计算的保存竞态可以干净地恢复，不再丢失工作内容（#2955）；复核的继续与展开行为趋于稳定（#2950）；不可用的准入会在放弃之前重试一次（#2943）。
- **智能体桥接** —— 视觉能力变化时 ACP 桥接会自动重连，会话不再因能力更新而卡住（#2963）。
- **工作区** —— 智能体提示处于活动状态时输入框保持忙碌状态，避免误发重复消息（#2836）；PDF 预览身份在首次发送时得到保留（#2948）。
- **设置** —— 全局与本地搜索快捷键已分离，不再互相冲突（#2934）；多语言连接器文案补全完毕（#2946）。
- **技能** —— 图片裁剪与修订流程恢复正常工作（#2936）。
- **存储** —— 历史数据位置在升级后得到持久化保存与保护（#2865）。
- **包与引导** —— 词元标志不再在无关单词内部被误匹配（#2940）；引导流程中的 DeepSeek 品牌标识已规范化（#2939）。
- **界面** —— 标注编辑提示框得到简化（#2966）；Windows 安装程序会以可操作的诊断信息报告清理失败（#2952）；标签页与列表中的文件类型图标已放大（#2976）；OpenAlex 连接器不再需要凭据，文献搜索开箱即可使用（#2969）。
