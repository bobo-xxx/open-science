## ✨ 亮点

- **Windows 上的 R 再度可靠。** 由 conda 管理的 R 在 Windows 上可靠启动，环境物化之后解释器重新得到解析，已验证的内核恢复会保留执行状态，封存配方检查则恢复已验证的 Windows pip 入口点。(#2630, #2645, #2653, #2679)
- **Notebook 回合可以正确重放。** 重放时会恢复同回合输入与标准库导入，因此重跑所见到的输入与原始运行完全一致。(#2706)
- **审阅者的修改保留上下文。** 关联的反馈线程不再在轮次之间丢失修改上下文，自动审阅在开启新对话后依然有效。(#2682, #2625)
- **更干净的文献与连接器证据。** PubMed 导入将作者姓氏与名字缩写区分开（并不再把 “Jr.” 之类的后缀当作名字缩写），对通过 Crossref 更新浮现的撤稿链进行检查，Ensembl、VEP、Reactome、OLS、CellGuide、UCSC 与 gnomAD 的行为更加精确。(#2675, #2677, #2693, #2703, #2696, #2687, #2664, #2663, #2634)

## 🔧 改进

- DeepSeek V4.1 Flash 加入模型目录，旧会话的模型在更新后得以保留（#2660），Volcengine Ark 阵容也得到刷新（#2673）。
- 标签选中指示器的动画更加平滑。(#2638)
- 文献工具卡片得到统一，文献详情得到扩展。(#2670)

## 🐛 问题修复

- **Notebook 与计算**——Windows 上的 conda R 在沙箱失败可追踪的情况下再次启动（#2630）；环境物化后 R 可执行文件重新解析（#2645）；执行与已验证的内核恢复得到保留（#2653）；同回合输入与标准库导入可以重放（#2706）；已验证的 Windows pip 入口点在可复现性检查中恢复（#2679）；排队的 CLI 对话快照得以对账（#2676）；被动会话水合期间不再发生写入（#2640）。
- **文献**——PubMed 作者姓氏与名字缩写得到区分（#2675），作者后缀不再被当作名字缩写（#2677）；Crossref 的 updated-by 撤稿关系会得到检查（#2693）。
- **连接器**——Ensembl 查询会报告解析得到的物种（#2703）；VEP 区域查询会归一化到正链（#2696）；请求的 Reactome 物种得到遵守并验证（#2687）；OLS 失败被保留，关系分页得到验证（#2664）；CellGuide 数据获取失败会正确浮现（#2663）；UCSC 边界得到验证，gnomAD 限制得到收紧（#2634）。
- **会话与侧聊**——侧聊保持其应用挂载（#2680），且不再为准入等待主回合就绪（#2668）；委派的 OpenCode 子智能体在隔离的运行时中运行（#2652）。
- **技能与市场**——绑定专家的运行时技能得到正确准备（#2698）；市场在作者缺失时显示其来源（#2672）；已预配的 OpenCode 技能引用可读（#2651）。
- **权限与设置**——原生网页搜索的批准会在对话期间被记住（#2646）；关闭移动端导航时设置保持打开（#2658）；Go 会话身份纳入服务商探测（#2662）；原始图像细节在 responses 桥中得到归一化（#2650）；撤销保持在设置面板之上（#2641）；上下文相关的内联消息布局得到恢复（#2674）。
