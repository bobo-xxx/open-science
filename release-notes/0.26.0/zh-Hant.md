## ✨ 亮點

- **以 Slurm 在 HPC 叢集上執行。** 遠端運算主機新增按主機設定的執行模式——直接 SSH 或 Slurm——讓 Notebook 執行能以 Slurm 作業提交，並提供持久的提交、輪詢、復原、取消與清理，以及引導式設定技能，把叢集確切的設定步驟交給你或你的管理員。(#2238)
- **文獻有了專屬的參考文獻庫。** 以識別碼或檔案匯入題錄，整理成文獻集並連結到專案，比較並批次合併重複題錄，從公開的開放取用來源附加全文 PDF，並格式化引用且保留產物溯源。(#2236)
- **新服務商與更新的模型目錄。** Apodex 加入內建服務商，最新的 OpenAI 與 Anthropic 模型——GPT-6 Astra 與 Claude Fable 5.1——開箱即可選用。(#2147, #2175)
- **工具活動一眼可讀。** 產物寫入與 Notebook 控制項會在訊息與核准中呈現為精簡摘要卡片，而不是原始 JSON。(#2224)

## 🚀 新功能

- **Slurm 執行模式**——遠端運算主機在桌面應用程式、網頁存取與 Host SDK 中皆可按主機選擇直接 SSH 或 Slurm 執行，附遠端運算設定中的執行模式選擇器、用於安全復原的排程器端作業命名，以及附確切設定、修復與移除說明、由使用者管理的 Compute Environment Setup 技能。(#2238)
- **文獻庫**——文獻集、專案關聯、下載 PDF 的收件匣接受流程、垃圾桶、識別碼感知匯入、帶產物溯源的引用格式化、並排重複比較，以及保留附件的批次合併；可透過 Europe PMC、PMC、OpenAlex、arXiv 與 Unpaywall 尋找並附加全文 PDF——所有適用的來源皆以平行方式查詢——憑證為選填。(#2236, #2234, #2262)
- **Apodex 服務商**——官方 Apodex 模型（`apodex-1.1` 與 `apodex-1.1-mini`），具 262,144 詞元上下文，並為每個智能體框架正確路由。(#2147)
- **最新的 OpenAI 與 Anthropic 模型**——GPT-6 Astra（1,050,000 詞元上下文）與 Claude Fable 5.1（1,000,000 詞元上下文），支援推理強度。(#2175)
- **工具摘要卡片**——在訊息與權限核准中，為產物寫入與常見 Notebook 控制項顯示精簡易讀的卡片。(#2224)
- **擴大的安全預設權限**——例行唯讀檢查（Notebook 執行環境與狀態、記憶查詢、套件清單）以及已核准計畫的進度更新，不再以核准提示打斷你；授權仍在 Settings 中可見且可撤銷。(#2225)
- **預覽內容右鍵選單**——在預覽內按右鍵即可複製路徑、下載，或將本機檔案另存為產物；受管理產物可檢視溯源或返回原始上下文。(#1862)
- **核准中可展開技能文件**——技能載入核准可展開，在核准前呈現完整的技能文件。(#2056)
- **Task API 會話設定**——可從 CLI、HTTP API 或 SDK 檢視與更新會話的模型、推理強度、記憶與運算主機選擇，而不需送出提示。(#2121)
- **含大量資產的技能匯入**——內含數萬個檔案的大型技能套件能可靠且快速地匯入。(#2138)

## 🔧 改進

- 串流回應的轉譯更順暢：每個影格的額外開銷、重複的節奏控制與貼底跟隨的閃爍都已消除。(#2141)
- 長會話依然快速：會話資源用量已有上限，且不可變檔案驗證會被快取，讓版本歷史更快。(#2167, #2158)
- 已核准的會話計畫可在重新啟動與新嘗試後保留——作用中的計畫會從持久的會話狀態重建，而不是因延續錯誤而失敗。(#2152)
- 更新處理更為寬容：會尊重取消操作、保留已提供的更新，沒有安裝程式的平台會取得說明與手動下載連結。(#2245)
- 長會話依然回應順暢：送出提示不再重新掃描完整對話記錄，交接、預覽與視窗讀取也設有上限。(#2256, #2259)
- Notebook 執行環境可以使用自動套件鏡像。(#2127)

## 🐛 問題修復

- **Notebook 與執行環境**——受保護的 Windows REPL 啟動已還原（#2122）；Windows 的 R 偵測改為透過 Rscript（#2212），標準啟動會保留 stdin（#2118）；受管理執行環境狀態已隔離（#2217）；環境修復與套件結果會正確回報（#2214）；重複使用的儲存格語言會被遵循，執行輸入也會保留（#2206, #2207）；被捨棄的程式碼寫入串流會復原（#2205）；安裝程式快取會留在受管理執行環境的儲存空間內（#2190）；核心活動也會在執行寫入失敗後穩定下來。
- **檔案、產物與預覽**——預覽與發布保持同步（#2191, #2198）；發布期間的文字讀取會重試（#2154）；受管理資源會在分頁之間保持釘選（#2197）；收尾會在多訊息輪次中復原（#2187）；獨立附件會在發布後正確返回（#2196）；CSV 計數與 Unicode 標題搜尋已修正（#2178）；完成列也不再因高度變化而閃爍。(#2153)
- **會話與持久化**——分支上下文可在分支失敗後保留（#2213）；延遲載入期間以較新的中繼資料為準（#2209）；串流區塊可在時鐘回撥後保留（#2159）；刪除邊界與補償失敗會被保留（#2193, #2165）；保留的產物仍可讀取（#2160）；停滯的服務商刪除會逾時（#2149）；並行的專案刪除會被隔離（#2172）；訊息快照也會持久保存。(#2161)
- **智能體、服務商與委派**——服務商狀態可在非同步完成後保留（#2232），驗證會限定於模型目標（#2136）；工具內容與回應語意會被保留（#2233）；智能體行程的擁有權會在復原與權限等待期間維持（#2219）；委派結果與生命週期會被保留（#2220）；專家復原與已核准能力會被保留（#2226, #2221）；自訂服務商 URL 會被驗證，且重新導向會被拒絕。(#2124, #2126)
- **技能、連接器與記憶**——技能套件在編輯與匯入之間保持完整性（#2227, #2180）；連接器資料會被保留，憑證更新可復原（#2229）；連接器回應本體會被釋放，呼叫預算也會被執行（#2182）；記憶編輯受到保護，短搜尋詞仍持續可用。(#2185)
- **更新、平台與工作區**——Windows 安裝程式會預檢已鎖定的更新目標（#2155）；Linux 應用程式選單保持可見（#2139）；啟動等待有上限，結束會受保護直到執行環境交接（#2166, #2202）；套用失敗後會還原即時代理狀態（#2169）；診斷記錄已加固，狀態會重新整理（#2249）；懸停預覽提供更清楚的互動回饋（#2263）；無障礙、對比與鍵盤導覽也已還原。(#2176, #2177)
- **會話與歸檔**——歸檔會等到會話的運算作業真正完成後才進行，且歸檔與還原的前置條件會使用持久版本，延遲的命令因此無法趁隙通過；被拒絕的工作區刪除會在對話方塊內顯示其錯誤，並提供明確的重試。(#2258)
- **儲存、憑證與遠端存取**——資料位置遷移會保留參照與復原語意（#2257）；憑證復原已還原，已儲存的狀態會重新對齊（#2260）；遠端存取可從持久化與探測失敗中復原（#2254）；用量記錄也會保留其執行識別並可復原。(#2248)
