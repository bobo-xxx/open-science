## ✨ 重点更新

- **xAI (Grok) OAuth 订阅。** 一个订阅账号可通过 xAI Responses API 同时用于 Claude Code（Anthropic Messages）、OpenCode（Chat Completions）和 Codex（Responses）三种智能体协议。设置和新手引导均支持设备代码登录，主进程会刷新令牌，并提供本地 Anthropic 词元计数估算；默认模型为 Grok 4.6。 (#1554, #1556)
- **受管治的市场专家。** 从市场安装的软件包带有明确的 `marketplace` 来源。发布者内容为只读，禁止通过 ZIP 手动覆盖；更新版本必须具有更高的 SemVer，并与精确的内容基线比较。“已安装”页面现按“全部 / 自定义 / 市场 / 内置”分组，并提供受管详情视图。 (#1600)
- **Notebook 跨运行依赖跟踪。** 已完成的 Python 和 R 运行会在进程内通过 tree-sitter WASM 进行分析，因此基于较早变量状态生成的输出会被标记为 `stale`、`clear` 或 `unknown`，不会再静默呈现与当前状态不符的结果。 (#1553)
- **运行中立即发送。** 在一轮任务运行期间发送排队消息不再中断当前任务。兼容层会在可用时使用各框架原生的后续消息转向能力，在不支持转向时平稳降级。 (#1566, #1593, #1592, #1590)
- **摘要优先的会话启动。** 会话查询元数据和每轮词元用量会物化到 SQLite 中。正常启动时只读取摘要，仅在打开或导出会话时加载对应的会话文件，不再解析所有会话 JSON。 (#1618, #1631)

## 🚀 新功能

- **xAI (Grok) OAuth 订阅提供商**，支持应用自有的设备授权、令牌刷新、本地词元计数估算，并将 Grok 4.6 设为默认模型。 (#1554, #1556)
- **受管治的市场安装专家**，提供只读发布者内容、SemVer 更新保护、受管详情、可编辑副本和针对性的卸载控制。 (#1600)
- **Notebook 跨运行依赖跟踪**，覆盖 Python 和 R 的别名、突变、类与对象模型以及常见科学计算库的影响。 (#1553)
- **通过原生后续消息实现运行中立即发送**，并提供框架感知的降级行为和正确的权限响应处理。 (#1566, #1593, #1592, #1590, #1589)
- **消息产物预览**，支持受管文件链接、Markdown 产物图像、稳定的产物/版本 ID 和生成卡片。 (#1587, #1597)
- **Notebook 预览改进**，覆盖图形、当前会话输出、Notebook 可用状态和已终止 Notebook 的只读预览。 (#1605, #1564, #1545, #1599)
- **上下文压缩转录边界**，为进行中、已完成、失败和已取消提供不同语义。 (#1581)
- **归档键盘撤销**，可使用标准桌面撤销快捷键恢复最近一次归档。 (#1595)
- **“关于”页面重新设计和反馈入口**，新增帮助中心与发行说明资源。 (#1551, #1588)
- **自定义模型词元限制**，分别配置上下文、输入和输出，并提供可编辑预设。 (#1525, #1546)
- **完整的 OAuth 连接器生命周期**，覆盖首次保存授权、重试、恢复、取消和稍后完成。 (#1560, #1563)
- **deepseek-v4-flash-vision-exp** 加入 DeepSeek 模型目录。 (#1538)

## 🔧 改进

- 会话元数据在 SQLite 中建立索引以支持摘要优先启动，并减少渲染器历史扫描。 (#1618, #1631, #1626)
- 批量处理工具突发和实时非文本事件，降低渲染器 IPC 负载。 (#1557, #1555)
- 安装器日志改为批量发送，并限制渲染器保留量。 (#1606)
- 提升完整 Vitest 测试工作进程和长对话 E2E 截图的稳定性。 (#1625, #1627, #1628)
- 澄清 v0.18.2 README 中的提示内容。 (#1634)

## 🐛 问题修复

- **智能体身份和能力范围**现在能够正确隔离。 (#1617)
- **实时“立即发送”**不再导致恢复或中断操作挂起。 (#1613)
- **Notebook 内核和标签页**现在能正确路由输入、保留已批准状态、限制协议输出、拒绝格式错误的调用，并处理 Python/R 运行时边缘情况。 (#1604, #1619, #1615, #1612, #1570, #1571, #1569, #1616, #1621, #1568, #1540, #1537)
- **Codex 和 Claude 提供商故障**现在能处理不兼容、暂时性、旧版槽位和持久连接问题，避免产生不必要的报告。 (#1594, #1586, #1584, #1583)
- **编辑器和队列**会保留斜杠快捷方式、专家选择、运行时准入消息和已保留的计划修订。 (#1633, #1630, #1601, #1610)
- **委派历史**会标记导入但未完成的子智能体，并避免重放活动冲突。 (#1609, #1520)
- **更新器和启动流程**会保留下载交接、分离数据库检查与组合延迟、保持加载动画连续，并允许关闭恢复覆盖层。 (#1632, #1539, #1598, #1565, #1591)
- **会话和工作空间**会正确限定模型范围、保留本地标题、拒绝重叠的空闲发送、刷新可达性并提供专家切换恢复。 (#1552, #1579, #1577, #1602, #1543)
- **设置和过期状态处理**会保留乐观偏好设置，并拒绝过期的预览、标签和文件夹刷新快照。 (#1629, #1573, #1578, #1574, #1575)
- **产物、计算、连接器和导航**改进了终结恢复、重试去重、暂时性 MCP 连接恢复、专家控件对齐和意外故障本地化。 (#1542, #1541, #1544, #1611, #1607, #1608, #1580)
- **Notebook 静态上下文**会保持在预算范围内。 (#1572)
